/**
 * A .ts file that contains a default options.
 */

/**
 * A default headers for apis.
 */
export const DEFAULT_HEADERS = {

};

/**
 * A default params for apis.
 */
export const DEFAULT_PARAMS = {

};