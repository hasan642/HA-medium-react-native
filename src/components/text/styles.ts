import { StyleSheet } from 'react-native';
import {
    color,
    typography
} from 'theme';

/**
 * A styles of text live here.
 */
const styles = StyleSheet.create({
    text: {
        color: color.dark,
        fontFamily: typography.primary
    }
});

/**
 * export as default.
 */
export default styles;